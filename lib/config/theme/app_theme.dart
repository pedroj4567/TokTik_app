import 'package:flutter/material.dart';

class AppTheme {
  // Retornamos un tema con la clase con este metodo
  // retora una instancia de themeData
  ThemeData getTheme() => ThemeData(
        brightness: Brightness.dark,
      );
}
